# licence check still pending on the font binaries — same reason Font/ is untracked
public/fonts/*
!public/fonts/88Sans-Grit.woff2
!public/fonts/88Sans-Clean.woff2
!public/fonts/PPFormula_ExtReg_ALT.woff2
!public/fonts/PPFormula_ExtBold_ALT.woff2
.env*.local
.next/
node_modules/
tsconfig.tsbuildinfo
next-env.d.ts
# the comment tool's written output, not app/api/feedback/ — anchored so the
# route source stays tracked
/feedback/
